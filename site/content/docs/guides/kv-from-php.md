+++
title = "KV from PHP"
weight = 6
+++

> **Stub** — not yet written. The `examples/README-KV.md` file in the repo has the current API surface.

## What this will cover

- The `ephpm_kv_*` SAPI functions exposed to PHP
- Strings, counters, TTLs
- Hash operations
- Compression behavior (transparent gzip/zstd/brotli)
- Comparison with `phpredis` / `predis` for migration
