+++
title = "ephpm php"
weight = 2
+++

> **Stub** — not yet written.

## What this will cover

- Synopsis: `ephpm php [PHP_ARGS...]`
- Running PHP CLI through the embedded runtime (no external `php` needed)
- Worked examples: `ephpm php -r '...'`, `ephpm php artisan migrate`, `ephpm php wp-cli.phar plugin list`
- Available extensions (matches the embedded runtime's compile flags)
- Windows-specific: DLL extraction
