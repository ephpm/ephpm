+++
title = "ephpm serve"
weight = 1
+++

> **Stub** — not yet written.

## What this will cover

- Synopsis: `ephpm serve [--config PATH] [--listen ADDR] [--document-root DIR] [-v...]`
- Default behavior (when invoked as `ephpm` with no subcommand)
- Flag-by-flag breakdown
- Signal handling (graceful shutdown on SIGTERM/SIGINT)
- Exit codes
