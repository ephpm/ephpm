+++
title = "Environment Variables"
weight = 3
+++

> **Stub** — not yet written.

## What this will cover

- The `EPHPM_*` override convention via figment
- How env vars map onto TOML keys (e.g. `EPHPM_SERVER__LISTEN`)
- Precedence: file < env < CLI flag
- Common production overrides (listen address, document root, log level)
